<br><br>
<div class='container mx-auto'>
    <div class='row'>
        <div class='col-lg-6'>
            <h1>Is Identical: {{authentication_result.isIdentical}}</h1>
            <h1> Confidence: {{authentication_result.confidence}}</h1>
        </div>
    </div>
    <div class='row'>
        <div class='col-lg-4'>
            <img id='fresh' src='{{link_fresh_pic}}' />
        </div>
        <div class='col-lg-4'>
            <img id='old' src='{{link_registered_pic}}' />
        </div>
    </div>
</div>